import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mytazkiyah.platform',
  appName: 'MyTazkiyah',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
